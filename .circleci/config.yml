version: 2

jobs:
  tests:
    docker:
      - image: node:8.10.0

    working_directory: ~/hetmech-frontend

    steps:
      - checkout
      - run:
          name: Frontend tests
          command: |
            npm install karma-cli --global
            npm install

workflows:
  version: 2
  test:
    jobs:
      - tests
